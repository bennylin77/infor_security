<%= form_for @comment , :url=>{:action=>:comment, :controller=>:main } do |f| %>
  <% if @comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@comment.errors.count, "error") %> prohibited this s_closed from being saved:</h2>

      <ul>
      <% @comment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <h1>意見回饋</h1>
  <br>
  <div class="field">
    <%= f.label :主旨 %><br />
    <%= f.text_field :subject, :size=>80 %>
  </div>
  <div class="field">
    <%= f.label :內容 %><br />
    <%= f.text_area :content, :size=>'150x3' %>
  </div>
  <div class="actions">
    <%= f.submit "送出" %>
  </div>
<% end %>