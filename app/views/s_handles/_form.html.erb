<%= form_for(@s_handle) do |f| %>
  <% if @s_handle.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@s_handle.errors.count, "error") %> prohibited this s_handle from being saved:</h2>

      <ul>
      <% @s_handle.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
  	工作編號:
  	<%= @s_handle.job_id %>
  </div>
  <div class="field">
    *<%= f.label :IP使用人處理記錄 %><br />
    <%= f.text_area :handling_description, :size=>'150x3'  %>
  </div>
  <div class="actions">
    <%= f.submit "送出" %>
  </div>
<% end %>
